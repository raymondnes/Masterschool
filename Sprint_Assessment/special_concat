# Concat

# Define a function named special_concat that accepts 3 strings (string). 
# The function should concat all the strings and return the result in the following format

# print(special_concat(”Cats”, “Dogs”, “Wolfs”))
# ”Cats, Dogs & Wolfs

def special_concat(str1, str2, str3):
  return(f"{str1}, {str2} & {str3}")

print(special_concat("Ray", "Neola", "Raynell"))
